<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required on Android 11+ (API 30+) so CampusMapScreen's
         mapIntent.resolveActivity(...) check can actually see the Google Maps
         package; without this, package visibility rules make it return null
         even when Maps is installed, silently breaking the "open in Google
         Maps" preference (see https://g.co/dev/packagevisibility). -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Punla"
        android:theme="@style/Theme.Punla"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|screenLayout|screenSize|smallestScreenSize"
            android:theme="@style/Theme.Punla">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Floating quick-add card launched from the Budget widget's +
             button; not a launcher entry point, so no intent-filter. -->
        <activity
            android:name=".ui.QuickAddExpenseActivity"
            android:exported="false"
            android:theme="@style/Theme.Punla.QuickAdd"
            android:excludeFromRecents="true"
            android:launchMode="singleTask" />

        <!-- Next Class widget -->
        <receiver
            android:name=".widget.NextClassWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_class_widget_info" />
        </receiver>

        <!-- Budget balance widget -->
        <receiver
            android:name=".widget.BudgetWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/budget_widget_info" />
        </receiver>

        <!-- Next deadline widget -->
        <receiver
            android:name=".widget.NextDeadlineWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_deadline_widget_info" />
        </receiver>

        <receiver
            android:name=".notification.NotificationInteractionReceiver"
            android:exported="false" />

        <receiver
            android:name=".worker.ClassDayNotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".pomodoro.PomodoroAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".pomodoro.PomodoroBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Background push (FCM) -->
        <service
            android:name=".push.PunlaFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
